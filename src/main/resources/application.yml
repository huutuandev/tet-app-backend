spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tet_fun_app?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none   # giữ update để tự tạo bảng khi thêm field mới
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  security:
    oauth2:
          client:
            registration:
              google:
                client-id: 696874933854-vtkdq99ej0j475rp8s309ncgigrpi96q.apps.googleusercontent.com
                client-secret: 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
                scope: openid,profile,email
                redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"  # quan trọng, để Spring tự handle
            provider:
              google:
                authorization-uri: https://accounts.google.com/o/oauth2/auth
                token-uri: https://oauth2.googleapis.com/token
                user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
                user-name-attribute: sub

app:
  jwt:
    secret: NguyenHuuTuan_dev_website-180505/01072023  # thay bằng chuỗi dài ngẫu nhiên
    expiration-ms: 86400000  # 24 giờ
