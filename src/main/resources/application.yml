spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tet_fun_app?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  # Redis
  data:
    redis:
      host: localhost
      port: 6379

  # Kafka - FIX CHỖ NÀY
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: tet-email-verification-group
      auto-offset-reset: latest  # ⚠️ ĐỔI từ "earliest" → "latest"
      enable-auto-commit: false  # ⚠️ THÊM dòng này
      properties:
        spring.json.trusted.packages: "*"  # ⚠️ THÊM dòng này
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  # Email
  mail:
    host: smtp.gmail.com
    port: 587
    username: hung0935347609@gmail.com
    password: ooos qvav olob viqm
    properties:
      mail.smtp:
        auth: true
        starttls.enable: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 696874933854-vtkdq99ej0j475rp8s309ncgigrpi96q.apps.googleusercontent.com
            client-secret: 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
            scope: openid,profile,email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub

app:
  jwt:
    secret: NguyenHuuTuan_dev_website-180505/01072023
    expiration-ms: 86400000
  email:
    verification:
      expiration-minutes: 15
      code-length: 6